import { WebApplication } from 'chen/web';
import { Provider } from 'chen/core';

/**
 * AppProvider class
 */
export class AppProvider extends Provider {

  /**
   * Register dependencies
   * @param {WebApplication} app
   */
  public async register(app: WebApplication) {

  }
}
