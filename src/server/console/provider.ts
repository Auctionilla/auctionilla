import { ConsoleApplication } from 'chen/console';
import { Provider } from 'chen/core';

/**
 * AppProvider class
 */
export class AppProvider extends Provider {

  /**
   * Register dependencies
   * @param {ConsoleApplication} app
   */
  public async register(app: ConsoleApplication) {

  }
}
